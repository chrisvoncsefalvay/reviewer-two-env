name: Test and Publish Purple Agent

on:
  pull_request:
    paths:
      - 'research_plan_env/purple_agent/**'
      - 'Dockerfile.purple-agent'
      - '.github/workflows/purple-agent-publish.yml'
  push:
    branches:
      - master
      - main
    tags:
      - 'v*'

jobs:
  test-and-publish:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/purple-agent
          tags: |
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.purple-agent
          push: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          load: true
          platforms: linux/amd64

      - name: Start agent container
        run: |
          # Start without API key - agent will start but won't be able to process requests
          # This tests that the server starts correctly
          docker run -d -p 9010:9010 --name purple-agent-container \
            $(echo "${{ steps.meta.outputs.tags }}" | head -n1) \
            --host 0.0.0.0 --port 9010
          echo "Waiting for agent to start..."
          timeout 120 bash -c 'until curl -sf http://localhost:9010/.well-known/agent.json > /dev/null 2>&1; do sleep 2; done' || {
            echo "Agent failed to start within timeout"
            docker logs purple-agent-container
            exit 1
          }
          echo "Agent is ready"

      - name: Verify agent card
        run: |
          echo "Agent card:"
          curl -s http://localhost:9010/.well-known/agent.json | jq .

      - name: Stop container and show logs
        if: always()
        run: |
          echo "=== Purple Agent Container Logs ==="
          docker logs purple-agent-container || true
          docker stop purple-agent-container || true

      - name: Log in to GitHub Container Registry
        if: success() && github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Docker image
        if: success() && github.event_name != 'pull_request'
        run: docker push --all-tags ghcr.io/${{ github.repository_owner }}/$(echo ${{ github.repository }} | cut -d'/' -f2)/purple-agent

      - name: Output image digest
        if: success() && github.event_name != 'pull_request'
        run: |
          echo "## Purple Agent Docker Image Published" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tags:** ${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Note:** Requires ANTHROPIC_API_KEY environment variable at runtime" >> $GITHUB_STEP_SUMMARY
